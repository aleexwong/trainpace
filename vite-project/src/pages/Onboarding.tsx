/**
 * Onboarding Page
 * Thin wrapper that renders the goal-setting wizard.
 */

import { OnboardingFlow } from "@/features/goals";

export default function Onboarding() {
  return <OnboardingFlow />;
}
